---
import Layout from "../layouts/Layout.astro";
---


<Layout title="Astro" description="Astro">
<div class="slider-container">
  <div class="slider">
    <div class="slide">
      <img src="/1.png" alt="슬라이드 1">
      <h2 class="slider-title">일본 편의점 꿀팁<br>일본에 갈때 꼭 먹어야할 편의점 음식 추천</h2>
    </div>
    <div class="slide">
      <img src="/2.png" alt="슬라이드 2">
      <h2 class="slider-title">일본 편의점 꿀팁<br>일본에 갈때 꼭 먹어야할 편의점 음식 추천</h2>
    </div>
    <div class="slide">
      <img src="/3.png" alt="슬라이드 3">
      <h2 class="slider-title">일본 편의점 꿀팁<br>일본에 갈때 꼭 먹어야할 편의점 음식 추천</h2>
    </div>
    <div class="slide">
      <img src="/4.png" alt="슬라이드 4">
      <h2 class="slider-title">일본 편의점 꿀팁<br>일본에 갈때 꼭 먹어야할 편의점 음식 추천</h2>
    </div>
    <div class="slide">
      <img src="/5.png" alt="슬라이드 5">
      <h2 class="slider-title">일본 편의점 꿀팁<br>일본에 갈때 꼭 먹어야할 편의점 음식 추천</h2>
    </div>
  </div>
  <button class="prev-button">이전</button>
  <button class="next-button">다음</button>
</div>

<div class="gm-container">
  <img src="/gm.png" alt="GM" class="gm-image">
</div>

<style>
  .slider-container {
    position: relative;
    width: 90%;
    max-width: 90vw;
    margin: 0 auto;
    overflow: hidden;
    height: 100vh;
  }

  .slider {
    display: flex;
    transition: transform 0.5s ease-in-out;
    height: 100%;
  }

  .slide {
    min-width: 100%;
    height: 100%;
    position: relative;
  }

  .slide img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: brightness(0.5); /* 채도 증가 및 어둡게 조정 */
  }

  .slider-title {
    position: absolute;
    top: 30%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    color: white;
    font-size: 50px;
    line-height: 1.4;
    font-weight: bold;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
    width: 100%;
  }

  .prev-button, .next-button {
    position: absolute;
    bottom: 10px;
    background-color: rgba(0, 0, 0, 0.5);
    color: white;
    border: none;
    padding: 8px 16px;
    cursor: pointer;
    border-radius: 4px;
    font-size: 12px;
    font-weight: bold;
  }

  .prev-button {
    left: 10px;
  }

  .next-button {
    right: 10px;
  }

  .gm-container {
    position: fixed;
    bottom: 15px;
    left: 15px;
    z-index: 10;
  }

  .gm-image {
    width: 80px;
    height: auto;
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const slider = document.querySelector('.slider') as HTMLElement;
    const slides = document.querySelectorAll('.slide');
    const prevButton = document.querySelector('.prev-button');
    const nextButton = document.querySelector('.next-button');

    if (slider && slides.length > 0 && prevButton && nextButton) {
      let currentIndex = 0;

      function showSlide(index: number) {
        if (index < 0) index = slides.length - 1;
        if (index >= slides.length) index = 0;
        slider.style.transform = `translateX(-${index * 100}%)`;
        currentIndex = index;
      }

      prevButton.addEventListener('click', () => showSlide(currentIndex - 1));
      nextButton.addEventListener('click', () => showSlide(currentIndex + 1));
    } else {
      console.error('슬라이더 요소를 찾을 수 없습니다.');
    }
  });
</script>
</Layout>
