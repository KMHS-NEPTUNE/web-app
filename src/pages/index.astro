
---
import { supabase } from '../lib/supabase';

const { data, error } = await supabase.from("todos").select('*');
---

{
  (
    <ul>
      {data?.map((entry) => (
        <li>{entry.name}</li>
      ))}
    </ul>
  )
}
